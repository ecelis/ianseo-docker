domain: ianseo.arqueria.pro       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://ianseo.arqueria.pro  # the base hostname and protocol for your site, e.g. http://example.com
#baseurl: /      # place folder name if the site is served in a subfolder
#theme: jekyll-theme-tactile
remote_theme: pages-themes/tactile@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one